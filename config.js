// ============================================================
// RENI NOW LEARNING — Supabase config
//
// You do NOT need to edit this file when deploying the backend
// (server/index.js) to Render — it reads SUPABASE_URL and
// SUPABASE_ANON_KEY from environment variables you set in the
// Render dashboard, and app.js fetches them from /api/config
// automatically.
//
// Only fill these in if you're serving public/ as a plain static
// site with no backend at all.
// ============================================================
window.RENI_CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT-REF.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-PUBLIC-KEY"
};
